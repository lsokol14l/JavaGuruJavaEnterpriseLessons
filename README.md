# Занятие 9. SQL часть 2.

Нормализация, 1-3 НФ, связи one to one, many to many, one to many, join(inner join), left/right join, cross(декартово супер редко используется), full join, group by/having, alter table, alter column

Опять же все база, про все итак прекрасно знал, если что-то подзабыл то гуглится за 2 секунды проблем нет.
